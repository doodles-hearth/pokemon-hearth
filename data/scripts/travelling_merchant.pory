script TravellingMerchant_EventScript_Keke {
	lock
	faceplayer

	setseenmon(SPECIES_KECLEON)
	playmoncry(SPECIES_KECLEON, CRY_MODE_NORMAL)

	if (flag(FLAG_MET_TRAVELLING_MERCHANT)) {
		setspeaker(SP_NAME_OKADAS_KECLEON)
	} else {
		setspeakertomonname(SPECIES_KECLEON)
	}

	switch(random(3)) {
		case 0:
			msgbox(format(
				"Keckecke!"
			))
		case 1:
			msgbox(format(
				"Klehhh!"
			))
		case 2:
			msgbox(format(
				"…"
			))
	}

	waitmoncry
	closemessage

	release
}
