mapscripts SpindaIsland_Home_MapScripts {
	
}

script SpindaIsland_Home_EventScript_HidingBoy {
	msgbox(format(
		"Something is blocking the way."
	), MSGBOX_NPC)
}

script testytesto {
	lock
	msgbox(format("SPEEEEN"))
	call(Spinda_SpinClockwise)
	release
}